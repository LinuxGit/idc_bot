<%= form_with(model: server_size, local: true) do |form| %>
  <% if server_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(server_size.errors.count, "error") %> prohibited this server_size from being saved:</h2>

      <ul>
      <% server_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :size_name, "Server type" %>
    <%= form.text_field :size_name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :model %>
    <%= form.select :model, ServerSize.models.keys, {}, { class: "custom-select" } %>
  </div>

  <div class="form-group">
    <%= form.label :cpu, "CPU" %>
    <%= form.text_field :cpu, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :cpu_cores, "CPU cores" %>
    <%= form.number_field :cpu_cores, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :memory %>
    <%= form.number_field :memory, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :disk %>
    <%= form.text_area :disk, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.submit "Submit", class: "btn btn-success" %>
  </div>
<% end %>
